# Video Splitter Container Dependencies
# Note: Standard library modules (json, os, subprocess, etc.) are not listed

# AWS SDK for Python - better error handling than AWS CLI
boto3>=1.34.0

# Type checking (development only, not installed in container)
# mypy>=1.8.0
